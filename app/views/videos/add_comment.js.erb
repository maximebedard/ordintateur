<% if @comment.errors.present? %>
  $(".add-comment").replaceWith("<%= escape_javascript(render 'add_comment', comment: @comment, video: @comment.video) %>")
<% else %>
  $(".comments").append("<%= escape_javascript(render @comment) %>")
<% end %>
